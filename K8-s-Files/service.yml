apiVersion: v1
kind: Service

metadata:
  name: atharva-service

spec:
  type: NodePort

  selector:
    app: web

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080